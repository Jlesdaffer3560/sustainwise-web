// The non-web implementation — a no-op. Selected automatically by
// page_title.dart's conditional export whenever dart:html isn't available
// (i.e. compiling for native), so the native app never even sees the real
// implementation below.
void setPageTitle(String title) {}
